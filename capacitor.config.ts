import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cloudomair.usersconnect',
  appName: 'usersconnect-mobile',
  webDir: 'www'
};

export default config;
