import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cloudomair.usersconnect',
  appName: 'UsersConnect',
  webDir: 'www'
};

export default config;
